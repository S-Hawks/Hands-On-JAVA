spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/course_api
    username: postgres
    password: f1234F
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    hibernate:
      ddl-auto: create